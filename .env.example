PORT=3000

# Connection string copied from Supabase (Project Settings > Database > Connection string > URI)
DATABASE_URL=postgresql://postgres:password@db.project.supabase.co:5432/postgres
DB_SSL=true

# Optional manual configuration (use if you are not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=user_history
DB_USER=postgres
DB_PASSWORD=postgres
